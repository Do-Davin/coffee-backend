# Ubuntu Package Commands

## Update package list

sudo apt update

Use this before installing software.

## Upgrade installed packages

sudo apt upgrade -y

Use this to update installed packages.

## Install a package

sudo apt install package-name -y

Example:

sudo apt install git -y
